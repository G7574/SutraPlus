import { DecimalpointPipe } from './decimalpoint.pipe';

describe('DecimalpointPipe', () => {
  it('create an instance', () => {
    const pipe = new DecimalpointPipe();
    expect(pipe).toBeTruthy();
  });
});
